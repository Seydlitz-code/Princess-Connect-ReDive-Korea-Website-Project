'use strict';

/**
 * 사용법: 이 파일을 admin-credentials.local.js 로 복사한 뒤 ADMIN_BOOTSTRAP_PASSWORD 를 채우세요.
 * admin-credentials.local.js 는 .gitignore 되어 있어 저장소에 올라가지 않습니다.
 */
module.exports = {
  ADMIN_BOOTSTRAP_PASSWORD: '',
};
